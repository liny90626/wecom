# OpenClaw WeCom 插件 v2.5.110-115 变更简报

> [!NOTE]
> `v2.5.110-115` 是 LinKy fork 维护版。本版本聚焦新版 OpenClaw 插件 manifest 诊断修复，不改变 WeCom 运行时交互逻辑。

## 2026-06-27（v2.5.110-115）

### 修复

- 在 `openclaw.plugin.json` 顶层新增 `contracts.tools`。
- 声明当前入口实际注册的工具：
  - `wecom_doc`
  - `wecom_calendar`
  - `wecom_mcp`
- 修复新版 OpenClaw 对 `api.registerTool(...)` 与 manifest 工具契约不一致的诊断告警。

### 保护

- 新增 manifest 回归测试，确认运行时工具注册声明不会在后续打包中丢失。
- 本次不改 B1 Markdown 表格、B2 长文本分段、B3 新消息合并、reasoning 思考块和 Bot WS 投递逻辑。

## 推荐验证命令

```bash
npx vitest run index.test.ts
node scripts/patch-wecom-markdown-table.mjs --check
node scripts/patch-wecom-long-message.mjs --check
node scripts/patch-wecom-b3-merge-thinking.mjs --check
npm run build
npx vitest run
```
