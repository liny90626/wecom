# v2.7.260-20（2026-08-28，LinKy fork 候选）

状态：本地安全边界修正候选；待完成全量自测、打包、tag 与 fork 推送。

## 根因

`2.7.260-19` 已修正 MCP 到 CLI 的跨品类服务路由，但继续走查发现两个独立的边界问题：

1. 多账号配置使用自定义账号 ID 时，OpenClaw 传入的字面量 `default` 会被旧账号解析器当作默认别名，回退到第一个账号；CLI 工具因此可能拿错企业凭据。
2. CLI 返回的错误 stderr 或 endpoint 诊断属于外部进程输出，超时、exit 2 和普通失败分支此前有路径直接返回；若输出含 secret 或带凭证 URL，会泄露到模型上下文或日志。

## 修复

- 多账号且没有真实 `default` 账号时，拒绝 `default` 合成别名；仍允许显式真实账号 ID。
- 对 CLI 错误 stderr 做 secret 脱敏，并把 endpoint 诊断裁剪到 origin + path；成功路径和既有 MCP 路由不变。
- 增加多账号别名、跨渠道错误组合和泄露式 CLI 输出回归。

## 验证

- 聚焦回归：CLI tool `16/16`、MCP fallback `8/8`、版本 `1/1` 通过。
- 既有回归隔离复跑：`monitor.active 3/3`、`file-text.integration 5/5` 通过。
- 全量单 worker 尝试为 `53/56` 文件、`708/712` 用例；失败集中在既有的 `monitor.active` 2 条、`file-text` 1 条和 `gateway-sim` 1 条。前两项已隔离通过；`gateway-sim` 在当前主机负载下多次超过文件内固定的 30 秒预算，未修改生产代码或 timeout。此前相同链路在低负载环境曾通过，故保留为环境验证风险，不冒充全量绿。
- `npx tsc --noEmit`、`npm run build`、`npm run verify-dist`、B1/B2/B3 与 `git diff --check` 全过。
- 自测只使用 OpenClaw `2026.7.1-2`。

## 边界

Windows x64/ARM64 真机与真实企业微信网关/客户端仍未验证；官方没有 Windows ARM64 CLI 平台包。
